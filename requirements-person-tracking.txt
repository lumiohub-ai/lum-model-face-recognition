# Person Tracking & Phone Usage Detection - Dependencies
# For integration with existing face recognition system

# ============================================================================
# Deep Learning & Computer Vision
# ============================================================================
ultralytics>=8.0.0              # YOLOv8 (person detection, tracking, phone detection)
torch>=2.0.0                    # PyTorch (already in main requirements)
torchvision>=0.15.0            # Torchvision (already in main requirements)
opencv-python>=4.8.0           # OpenCV (already in main requirements)

# Note: insightface already installed in main requirements.txt
# We'll reuse the existing face recognition pipeline

# ============================================================================
# Database & Caching
# ============================================================================
psycopg2-binary>=2.9.0          # PostgreSQL client (already in main requirements)
redis>=5.0.0                    # Redis client for pub/sub

# Note: pgvector support is already configured in the main system

# ============================================================================
# API & Web Framework
# ============================================================================
fastapi>=0.104.0                # FastAPI for REST API
uvicorn[standard]>=0.24.0       # ASGI server
pydantic>=2.0.0                 # Data validation
pydantic-settings>=2.0.0        # Settings management

# ============================================================================
# Utilities
# ============================================================================
numpy>=1.24.0                   # Numerical computing (already in main requirements)
scipy>=1.10.0                   # Scientific computing
Pillow>=10.0.0                  # Image processing (already in main requirements)
python-dotenv>=1.0.0            # Environment variable loading (already in main requirements)
loguru>=0.7.0                   # Logging

# ============================================================================
# Storage & File Management
# ============================================================================
# Note: gcsfs already in main requirements for GCS support

# ============================================================================
# Testing (Development)
# ============================================================================
pytest>=7.4.0                   # Testing framework
pytest-asyncio>=0.21.0          # Async testing support
pytest-cov>=4.1.0               # Coverage reporting
pytest-mock>=3.12.0             # Mocking support

# ============================================================================
# Additional Utilities
# ============================================================================
PyYAML>=6.0.0                   # YAML configuration
python-multipart>=0.0.6         # File uploads for FastAPI
aiofiles>=23.2.0                # Async file operations
