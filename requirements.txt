# ML models (face detection, person tracking, ReID, action recognition), now
# a standalone package at github.com/lumiohub-ai/lum-model-vision. Pinned to a
# tag, not a branch, so installs are reproducible — bump the tag deliberately,
# don't float it. Private repo: this needs an SSH agent forwarded at install
# time (docker build --ssh default; see Dockerfile).
# Brings in opencv, ultralytics, onnxruntime, loguru, gdown, lap, filterpy,
# ollama and boxmot.
lum-model-vision @ git+ssh://git@github.com/lumiohub-ai/lum-model-vision.git@v0.1.0

# To iterate on model code locally instead, clone the package repo alongside
# this one and comment out the line above in favor of:
#   -e ../lum-model-vision

# Application-only dependencies
requests~=2.32.3
psutil~=6.1.1
nvidia-ml-py~=12.560.30  # GPU monitoring via pynvml (nvmlDeviceGetUtilizationRates)
dotenv~=0.9.9

# pgvector Database dependencies
psycopg2-binary~=2.9.9
sqlalchemy~=2.0.23
pgvector~=0.2.4

# Redis for Pub/Sub (MDA)
redis~=5.0.1

# Celery for async task processing (MDA)
celery~=5.4.0
flower~=2.0.1

# GCS image fetching
google-cloud-storage~=2.14.0
