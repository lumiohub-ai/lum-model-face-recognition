# ML models (face detection, person tracking, ReID, action recognition).
# Editable install: edits under packages/lum-model-vision/ take effect without reinstalling.
# Brings in opencv, ultralytics, onnxruntime-gpu, loguru, gdown, lap, filterpy,
# ollama and boxmot — see packages/lum-model-vision/pyproject.toml.
-e ./packages/lum-model-vision

# Application-only dependencies
requests~=2.32.3
psutil~=6.1.1
nvidia-ml-py~=12.560.30  # GPU monitoring via pynvml (nvmlDeviceGetUtilizationRates)
dotenv~=0.9.9

# pgvector Database dependencies
psycopg2-binary~=2.9.9
sqlalchemy~=2.0.23
pgvector~=0.2.4

# Redis for Pub/Sub (MDA)
redis~=5.0.1

# Celery for async task processing (MDA)
celery~=5.4.0
flower~=2.0.1

# GCS image fetching
google-cloud-storage~=2.14.0
