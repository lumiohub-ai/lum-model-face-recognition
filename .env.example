# Minimal .env for R&D mode
# Copy to .env:  cp .env.example .env

FR_SLUG=face-recognition
USE_PGVECTOR=false
PRODUCTION=false
COMPOSE_FILE=compose.rnd.yml
# Path to large external dataset directory on the host
DATA_ROOT=/mnt/data/your-data
